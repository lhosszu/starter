spring:
  main:
    banner-mode: off
  datasource:
    url: jdbc:h2:file:./db/starter
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
