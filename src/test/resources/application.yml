spring:
  main:
    banner-mode: off
  profiles:
    active: test
  datasource:
    url: jdbc:h2:file:./db/starter-test
    username:
    password:
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
